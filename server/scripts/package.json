{
  "name": "basikal-gaming-server",
  "version": "1.0.0",
  "description": "BASIKAL GAMING backend for auth, wallet, and Card Battle multiplayer",
  "main": "server.js",
  "scripts": {
    "check": "node --check server.js && node --check storage.js && node --check deckUtils.js && node --check gachaCards.js && node --check scripts/share-ngrok.js",
    "start": "node server.js",
    "dev": "node server.js",
    "share": "node scripts/share-ngrok.js",
    "tunnel": "ngrok http 3000"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.7",
    "express": "^4.18.2",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.2.0",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.13.1",
    "socket.io": "^4.7.4"
  }
}
